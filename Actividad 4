from abc import ABC, abstractmethod

class Notificacion(ABC):

    @abstractmethod
    def enviar(self):
        pass


class Email(Notificacion):

    def enviar(self):
        return "La notificación fue enviada por email."


class SMS(Notificacion):

    def enviar(self):
        return "La notificación fue enviada por SMS."
